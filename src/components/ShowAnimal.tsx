import { useParams } from "react-router-dom";

/* export const ShowAnimal = () => {
  const { id } = useParams();

  return (
    <section className="animalCard">
    <img src={img} alt={name} />
    <p>{Name}</p>
    <p>{yearofbirth}</p>
    <button>Om {name}</button>
    <span>{medicin}</span>
    <span>Matad</span>
    <span>Senast matad</span>
    <h2>Now showing post {id}</h2>
   </div>
   </section>
  )
};
 */
